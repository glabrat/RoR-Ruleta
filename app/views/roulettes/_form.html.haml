= form_for @roulette do |f|
  - if @roulette.errors.any?
    #error_explanation
      %h2= "#{pluralize(@roulette.errors.count, "error")} prohibited this roulette from being saved:"
      %ul
        - @roulette.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
